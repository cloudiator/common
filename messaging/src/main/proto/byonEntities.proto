syntax = "proto3";

package iaas;

option java_package = "org.cloudiator.messages.entities";

enum QueryFilter {
  ALL = 0;
  ALLOCATED = 1;
  UNALLOCATED = 2;
}

enum CacheOperation {
  INSERT = 0;
  REMOVE = 1;
}
